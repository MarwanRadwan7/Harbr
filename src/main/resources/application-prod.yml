spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  flyway:
    clean-disabled: true

harbr:
  jwt:
    secret-key: ${HARBR_JWT_SECRET_KEY}

logging:
  level:
    root: WARN
    com.harbr: INFO
    org.hibernate.SQL: WARN